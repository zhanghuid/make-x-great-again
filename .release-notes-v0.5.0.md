# v0.5.0 — 被动零网络消费端构建

## 🎯 核心变化

**扩展现在完全离线运行** — 零网络请求

- ✅ 移除最后一条网络调用：误判申诉改为打开 GitHub Issue 模板（而非 POST `/v1/appeal`）
- ✅ 扩展消费端不再依赖任何远程端点，本地黑名单完全自给自足
- ✅ 符合 AMO 的 `data_collection: "none"` 声明

## 🔧 工程改进

### 扩展端 (v0.5.0)
- 删除过时代码：GraphQL 主世界拦截（x-graphql-main.content.ts）、GitHub Device Flow 登陆残留、whitelist 缓存、token bucket、noops 设置项
- 隐藏语义更正确：`拉黑` → `隐藏`（隐藏是本地操作，非 X 的 Block API）
- 修复 un-hide：隐藏路径现在写入 BlockRecords，removeBlock 清除两个存储，storage.onChanged 跨标签页传播
- 修复资料页无限徽章重挂载 + 数据膨胀
- XSS 加固：reasons/note/avatarUrl 在徽章气泡中转义
- 回收利用的 article anchors 延迟隐藏前重新验证
- 廉价-key-first 扫描跳过，SPA 导航清理页面状态，ctx-bound intervals/observer
- Manifest：名称「Make X Great Again (MXGA)」，Firefox 最低版本 109.0

### 工具链 & 边缘服务

**Bloom 过滤器自适应**
- 从条目数动态计算（之前固定值针对 10k 条 → ~78% FPR at 46k）
- 修复退化 h2、文档 wire-format spec、加 FPR + golden-vector 测试

**LLM 加固**
- JSON 编码不可信字段到显式 data-only 块（防 prompt-injection）
- Cap 长度，30s 超时，重试 429/5xx

**黑名单编译检查**
- 验证条目、删除 handle-only 行（handle 重用陷阱，138 行已删）
- 按 id dedup、label 检查

**公开 endpoints 加固**
- /v1/classify (20/h per identity/IP) 和 /v1/appeal (5/h per IP) 速率限制
- Timing-safe 管理员令牌对比
- Malformed 请求返回 400（而非 500）
- Zod 验证 /v1/admin/decide

**边缘定时任务拆分**
- GitHub 数据镜像仅在 6h 触发（之前每 10 分钟与 artifact 发布并行）

**Artifact URL-safe**
- 版本前缀映射到 URL-safe base64 字母表（'+' → '-', '/' → '_'）
- 修复 artifact 404 问题

## 🔒 隐私改进

**现在是 100% 离线** — 没有任何网络请求

| 功能 | 实现 |
|------|------|
| 列表匹配 | 本地 blacklist.json（bundled） |
| 误判申诉 | 打开 GitHub Issue 模板 |
| 隐藏操作 | 本地 DOM + BlockRecords 存储 |
| 权限 | storage only（无 cookies、IndexedDB） |

## 📊 架构变更总结

| 方面 | v0.4.0 | v0.5.0 |
|------|--------|--------|
| **消费端网络调用** | `/v1/check` | 无 |
| **申诉流程** | POST `/v1/appeal` | GitHub Issue 模板 |
| **隐藏实现** | X Block API(`blocks/create.json`) | 本地 DOM + 存储 |
| **GitHub 登陆** | Device Flow | 无 |
| **存储权限** | storage + cookies | storage only |

## 📝 升级指南

### Chrome Web Store 用户
- 商店审核通过后会自动推送，无需任何操作

### 手动加载 / Firefox / Edge / Brave 用户

**编译完毕后**，下载对应版本：
1. `mxga-extension-0.5.0-chrome.zip`（Chrome/Edge/Brave）
2. `mxga-extension-0.5.0-firefox.zip`（Firefox）
3. 解压后在扩展管理页加载已解压的扩展程序

## ✅ 测试覆盖

- ✅ TypeScript strict + Biome lint（全绿）
- ✅ 9 条边缘服务端点测试（全通过）
- ✅ Bloom 过滤器 FPR + golden-vector 测试
- ✅ 烟雾测试：probe 生成 + 失败时 non-zero exit
- ✅ Firefox 兼容性：最低版本 109.0

## 📚 相关文档

- `docs/STATUS.md` — 架构现状，已同步 v0.5.0
- `docs/PRIVACY.md` — 隐私政策（零网络数据流）
- `docs/DATA_USAGE.md` — blacklist schema 和消费路径
- `CHANGELOG.md` — 完整变更日志

---

**首次完全离线发布。** 现在的 MXGA 扩展是一个真正的"去中心化本地工具"——所有智能（黑名单匹配 + 隐藏管理）都在你的浏览器中运行，零外呼。🚀